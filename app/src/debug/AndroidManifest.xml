<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application>
        <!-- Debug-only entry point for repeatable stream Activity smoke tests. -->
        <activity
            android:name=".Game"
            android:exported="true"
            tools:replace="android:exported" />

        <provider
            android:name="com.limelight.nvstream.filetransfer.GenericContentProvider"
            android:authorities="${applicationId}.desktopfiletest"
            android:exported="false" />
    </application>
</manifest>
