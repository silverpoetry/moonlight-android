<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Native libraries use AGP's legacy compressed packaging for this minSdk and are
         therefore extracted at install time, as required by evdev_reader. -->
    <application android:label="@string/app_label_root" />
</manifest>
