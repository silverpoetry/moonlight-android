<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppBaseTheme" parent="android:Theme.Material">
        <item name="android:statusBarColor">#212121</item>
        <item name="android:navigationBarColor">#212121</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="StreamBaseTheme" parent="AppBaseTheme">
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- Stream activity theme -->
    <style name="StreamTheme" parent="StreamBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!-- A secondary display has no activity window behind it, so it owns one black fallback. -->
    <style name="SecondaryDisplayTheme" parent="StreamTheme">
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- API-specific stream-view behavior is added by version-qualified styles. -->
    <style name="StreamSurfacePlatformBehavior" />

    <!-- API 33+ asks the window manager to keep important overlays unobscured. -->
    <style name="StreamOverlayPlatformBehavior" />

    <declare-styleable name="ApertureViewGroup">
        <attr name="aperture_color1" format="color" />
        <attr name="aperture_color2" format="color" />
        <attr name="aperture_border_width" format="dimension" />
        <attr name="aperture_border_angle" format="dimension" />
        <attr name="aperture_duration" format="integer" />
        <attr name="aperture_middle_color" format="color" />
    </declare-styleable>


    <style name="BaseActivityTheme" parent="AppTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!--
        Settings root and detail screens are separate instances of the same
        activity on compact displays. Their motion belongs to the window
        theme so root entry, detail entry, detail return, and root exit share
        one transition contract on every vendor implementation.
    -->
    <style name="SettingsActivityTheme" parent="BaseActivityTheme">
        <item name="android:windowBackground">@color/moonlight_window_background</item>
        <item name="android:windowAnimationStyle">@style/SettingsActivityAnimation</item>
    </style>

    <!--
        API 29+ normally uses the light BaseActivityTheme. The user's dark
        appearance preference therefore needs a dark settings variant rather
        than the generic AppTheme, so both presentations keep the same opaque
        launch background and transition contract.
    -->
    <style name="SettingsActivityDarkTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/moonlight_window_background</item>
        <item name="android:windowAnimationStyle">@style/SettingsActivityAnimation</item>
    </style>

    <style name="SettingsActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/settings_activity_open_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/settings_activity_open_exit</item>
        <item name="android:activityCloseEnterAnimation">@anim/settings_activity_close_enter</item>
        <item name="android:activityCloseExitAnimation">@anim/settings_activity_close_exit</item>
    </style>

    <style name="FilePushTheme" parent="BaseActivityTheme">
        <item name="android:windowBackground">@color/moonlight_window_background</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="MoonlightDialogActivityTheme" parent="BaseActivityTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.45</item>
    </style>





    <style name="GameMenuRadioButton" parent="@android:style/Widget.CompoundButton.RadioButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginEnd">5dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@drawable/ic_game_menu_btn_selector</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">4dp</item>
        <item name="android:textColor">@color/game_menu_text_selector</item>
        <item name="android:textSize">10sp</item>
    </style>






    <style name="GameMenuSeekBar" parent="@android:style/Widget.Material.SeekBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:progressDrawable">@drawable/ic_game_menu_seek_bg</item>
        <item name="android:splitTrack">false</item>
        <item name="android:thumb">@android:color/transparent</item>
    </style>


    <style name="BottomDialog" parent="@android:style/Theme.DeviceDefault.Dialog">
        <item name="android:windowTitleStyle">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowAnimationStyle">@style/BottomDialog.AnimationStyle</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
    </style>


    <style name="BottomDialog.AnimationStyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/boxart_fadein</item>
        <item name="android:windowExitAnimation">@anim/boxart_fadeout</item>
    </style>

</resources>
