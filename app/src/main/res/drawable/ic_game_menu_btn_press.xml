<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 填充的颜色 -->
    <corners android:radius="16dp" />

    <!-- 边框的颜色和粗细 -->
    <solid android:color="@color/game_menu_material_primary_container" />

    <!-- android:radius 圆角的半径 -->
    <stroke
        android:width="0dp"
        android:color="@color/game_menu_material_outline_variant" />

</shape>
