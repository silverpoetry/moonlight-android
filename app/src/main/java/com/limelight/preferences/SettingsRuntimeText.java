package com.limelight.preferences;

/** Localized text port for runtime-derived settings metadata. */
interface SettingsRuntimeText {
    CharSequence clipboardDirectorySummary(String directoryLabel);
}
