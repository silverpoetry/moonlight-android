# 隐私说明

Moonlight for Android 是连接用户指定主机的串流客户端。项目不运营中转服务，也不包含
广告、用户分析或远程崩溃上报 SDK。串流需要网络权限，相关数据在客户端与用户选择的
GameStream/Sunshine 主机之间传输。

## 数据与用途

- **主机和配对信息**：主机名称、地址、端口、MAC、固定证书和客户端身份保存在应用
  私有目录，用于发现、唤醒、验证主机和维持配对。
- **应用设置**：视频、音频、输入、界面、文件目录和虚拟控件配置保存在本机。
- **剪贴板**：仅在用户开启剪贴板同步且串流会话完成能力协商后处理。文本或图片按剪贴板
  变化同步；文件和目录先交换轻量清单，真实内容在远端明确粘贴或拉取后读取。
- **麦克风**：需要 Android 录音权限和串流中的麦克风开关。采样编码后通过协商的会话
  通道发送到当前主机；应用不保存录音。
- **本地文件**：系统分享、剪贴板拉取和配置归档使用 Android Storage Access Framework
  或临时 URI 授权。应用只访问用户选择的文件和已授权目录。
- **诊断信息**：Debug 包可输出用于定位问题的状态日志。Release 日志限频并排除剪贴板
  正文、文件路径、证书、私钥、麦克风采样和逐事件实时轨迹。

## 备份与迁移

Android 自动备份排除所有 SharedPreferences、主机数据库、客户端证书/私钥和设备身份。
用户可以通过“导出配置”创建 ZIP；其中可能包含主机信息和客户端私钥，应按敏感文件
保管。导入界面允许选择 App 设置、主机连接信息和客户端身份，未选择的组件保持不变。

卸载应用会按 Android 规则删除应用私有数据；用户主动保存到共享存储的文件、下载内容
和配置 ZIP 不会随卸载自动删除。

## 权限说明

- `INTERNET`、网络和 Wi-Fi 状态：发现主机并建立串流；
- `CHANGE_WIFI_MULTICAST_STATE`（Android 13 及以下）：局域网 mDNS 发现；
- `RECORD_AUDIO`：用户启用麦克风上行时采集声音；
- `WAKE_LOCK`：串流期间维持必要的处理状态；
- `VIBRATE`：输入、设置控件和虚拟控制的触感反馈；
- Android TV EPG 权限：在支持的电视启动器中发布应用频道和封面。

权限的实际授予和使用取决于 Android 版本、设备能力和用户启用的功能。
